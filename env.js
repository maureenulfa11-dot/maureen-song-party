// env.js — Replace the placeholder values with your Supabase project URL and anon (public) key.
// WARNING: Do NOT commit your real env.js with private keys to a public repository. You may expose the anon public key
// for GitHub Pages, but never expose the service_role key. Keep env.js local or add it to your deployment as needed.

window.SUPABASE_ENV = {
  SUPABASE_URL: "https://your-project.supabase.co", // replace with your Supabase project URL
  SUPABASE_ANON_KEY: "your-anon-public-key" // replace with your anon/public key
};
